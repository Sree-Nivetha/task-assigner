---
description: how to setup and run the teacher task scheduler
---
# 🚀 Teacher Task Scheduler - Setup & Run Workflow

This workflow guides you through setting up the environment and running the Teacher Task Scheduler application.

// turbo-all
## 🛠️ Step 1: Install Dependencies
Run this command to install the required Node.js packages.
```bash
npm install
```

## 🏃 Step 2: Start the Application
Start the server in development mode (using nodemon) or standard mode.

### Option A: Development Mode (Auto-restart)
```bash
npm run dev
```

### Option B: Standard Mode
```bash
npm start
```

## 🌐 Step 3: Open in Browser
Once the server is running, visit:
[http://localhost:3000](http://localhost:3000)
